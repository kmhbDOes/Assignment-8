import React from "react";

const Blog = (props) => {
    const {blog-cover-Image,} = props.blog
    return <div>
        <p>
            <img src={} alt="" />
      </p>
  </div>;
};

export default Blog;
